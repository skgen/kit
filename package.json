{
  "name": "@skgn/kit-monorepo",
  "type": "module",
  "version": "1.0.0",
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "publish": "pnpm publish packages/kit --access public",
    "publish:dry": "pnpm publish packages/kit --access public --dry-run --report-summary"
  },
  "devDependencies": {
    "@antfu/eslint-config": "catalog:",
    "eslint": "catalog:",
    "eslint-plugin-format": "catalog:"
  }
}
